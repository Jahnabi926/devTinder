# NOTES

1. Go to mongo db website
2. Create a free M0 Cluster'
3. Create a user(collection) inside a database called "HelloWorld"
4. Get the connection string
5. Download/ install Mongodb compass . It's like a UI for database.
6. Create a database
7. Install Mongodb package
8. Create a connection from code in file database.js
9. Write and run Documents CRUD - Create , Read, Update , Delete, operations using "MongoDB Node.js Driver" documentation

# In database.js file

Make the connection, connect to the database and to the collection inside the database,
handles a promise and closes the client. Then perform CRUD operations.
